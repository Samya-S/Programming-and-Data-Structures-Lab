#include <stdio.h>

int main()
{
    // int m, n;
    // float a, b;
    // scanf("%d %d %f %f", &m,&n,&a,&b);
    // printf("The integers are %d & %d and the floating point no.s are %f & %f", m,n,a,b);

    // double c, d;
    // char x, y;
    // scanf("%lf %lf %c %c", &c,&d,&x,&y);
    // printf("The doubles are %lf & %lf and the characters are %c & %c", c,d,x,y);

    // float n;
    // scanf("%f", &n);
    // printf("%.3f",n);

    // int p, q, r, s;
    // scanf("%d %d %d %d", &p,&q,&r,&s);
    // printf("The integers are %10d, %10d, %10d, %10d", p,q,r,s);

    // int p, q, r, s;
    // scanf("%d %d %d %d", &p,&q,&r,&s);
    // printf("The integers are         %d,         %d,         %d,         %d", p,q,r,s);


    // char c='O';
    // printf("%d", c);

    return 0;
}